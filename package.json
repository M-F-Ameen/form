{
  "name": "job-application-form",
  "version": "1.0.0",
  "description": "Full-stack job application form with admin panel",
  "main": "backend/working-server.js",
  "scripts": {
    "start": "node backend/working-server.js",
    "dev": "nodemon backend/working-server.js",
    "build": "echo 'No build step required'",
    "setup": "npm install && mkdir -p backend/uploads"
  },
  "keywords": ["job-form", "nodejs", "mongodb", "express"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
